# 甲方需求 vs 项目进度对照表

> 更新时间：2026-04-11  
> 基于甲方需求表（22项功能点）逐项对照当前代码实现情况

## 状态图例

| 标记 | 含义 |
|------|------|
| ✅ | 已完成 |
| 🔧 | 部分完成，需补齐 |
| ❌ | 未开始 |

---

## 一、辅助著录模块（需求 01–06）

| # | 功能点 | 优先级 | 状态 | 说明 |
|---|--------|--------|------|------|
| 01 | 模板导入：支持导入 Excel 著录模板（含文号、责任者、日期、页数、密级、存放路径、各注字段） | 高 | 🔧 | 已有 `HEADERS` 定义在 `excel_export.py`，但缺少**从 Excel 导入**的反向功能。当前只支持导出。 |
| 02 | 自动识别填充：对已上传档案图片进行 OCR 识别，自动提取文号、责任者、题名、日期，密级字并填入著录表；页数根据图片数量自动统计 | 高 | ✅ | `excel_export.py` 规则提取 + `llm_field_extraction_service.py` LLM 提取均已实现，合并文档逻辑在 `batch_merge_extraction_service.py`。 |
| 03 | 页数自动计算：每件档案的图片数量自动计为页数，支持手工修改 | 高 | ✅ | Worker 自动统计 `page_count`，前端结果页可手动编辑。 |
| 04 | 批量修改：支持按条件（如年度、保管期限）批量更新责任者、密级等字段 | 高 | ❌ | 前端/后端均无批量修改归档字段的功能。需新增 API + 前端批量编辑 UI。 |
| 05 | 人工校对界面：高亮显示 OCR 低置信度字符，支持逐字修改或逐字段修改，并记录修改日志 | 高 | 🔧 | 结果页有逐页文本编辑；但**无低置信度高亮**，**无修改日志记录**。 |
| 06 | 著录表导出：支持导出为 Excel (.xlsx)、CSV，或通过 API 推送至数字档案管理系统 | 高 | 🔧 | Excel 导出 ✅（`ai-merge-export`）；CSV 导出 ❌；API 推送 ❌。 |

## 二、文件夹与归类模块（需求 07–12）

| # | 功能点 | 优先级 | 状态 | 说明 |
|---|--------|--------|------|------|
| 07 | 规则配置：管理员可配置档号组成规则（如：根目录/全宗/年度/保管期限/件号），支持自定义分段 | 高 | 🔧 | `_extract_archive_number` 硬编码了 WS/KJ 规则。缺少**管理员可配置的规则界面**。 |
| 08 | 自动建文件夹：根据档号规则自动生成5级目录（如：D:\档案\WS\2019\D10\0001），命名规则可参考著录流程 | 高 | ❌ | 当前不会自动创建物理文件夹。需新增档案归档目录生成服务。 |
| 09 | 图片自动排检：上传/拖拽图片，根据文件名中的件号或手动指定件号，自动将图片移动/复制到对应件号文件夹；支持批量导入 | 高 | 🔧 | `BufferZone.vue` 支持批量上传；但**不会按件号自动分拣到文件夹**。 |
| 10 | 档案识别与自动分件：系统对已扫描的档案图片进行归档章检测与识别，自动定位图片中归档章的位置，OCR 提取章内填写的信息（至少包括：全宗号、年度、保管期限（件号）、自动将图片归类到对应件/完整档号），并按 08、09 规则建立文件夹和移动图片。支持同一批次混合多件档案的批量自动分件。对于无法识别或归档章缺失的图片，应标记并转入人工处理队列 | 高 | 🔧 | 归档章检测在 `seal` 类型识别中部分支持；**自动分件逻辑未实现**；人工处理队列 = `human_review` 状态。缺少完整的分件工作流。 |
| 11 | 路径回写：自动为每件档案的完整存放路径写入著录表的"存放路径"字段 | 中 | ✅ | 本轮已添加 `storage_path` 字段到 Python/Java/DB。 |
| 12 | OCR 全文识别：对实际图片进行 OCR 识别，生成全文文本（简体中文、数字、英文）；支持增量更新 | 高 | ✅ | 三模型识别（VL/Layout/OCR）均已实现。增量 = 任务级重新提交。 |

## 三、全文检索模块（需求 13–17）

| # | 功能点 | 优先级 | 状态 | 说明 |
|---|--------|--------|------|------|
| 13 | 索引自动构建：对识别后的全文文本自动建立倒排索引，支持增量更新 | 中 | 🔧 | 当前用 PostgreSQL `LIKE` 搜索（本轮已增强 JOIN archive_records）。**无真正的倒排索引**（如 Elasticsearch）。对小规模数据够用。 |
| 14 | 关键词检索：支持模糊匹配、精确匹配，返回包含关键词的档案列表 | 高 | ✅ | `SearchPage.vue` + Java `searchWithArchive` 已实现模糊匹配，本轮新增了档案字段搜索。 |
| 15 | 检索结果展示：每条结果展示文号、题名、责任者、日期、密级、存放路径；关键词高亮显示 | 中 | ✅ | 本轮已完成：`SearchPage.vue` 展示档号/文号/责任者/日期/密级/存放路径 + 高亮。 |
| 16 | 定位与预览：点击结果可直接打开对应文件，或在线预览档案图片（需要成图片查看器） | 中 | 🔧 | 点击搜索结果可跳转结果页查看 OCR 内容 + 源图片。**缺少独立图片查看器**（放大/缩小/翻页）。 |
| 17 | 高级筛选：支持在检索结果中按年份、保管期限、密级、文号等字段进行二次过滤 | 中 | ❌ | 搜索页无高级筛选 UI。需新增筛选面板。 |

## 四、系统管理模块（需求 18–21）

| # | 功能点 | 优先级 | 状态 | 说明 |
|---|--------|--------|------|------|
| 18 | 用户与权限：查看角色（管理员/签录员/检索者），审批（权限赋予），密码管理 | 高 | ✅ | `AdminCenterPage.vue` 用户管理 + 角色设置 + 审批 + 密码重置均已实现。 |
| 19 | 操作日志：记录所有查看、修改、删除、检索操作，包含操作人、时间、IP、操作内容 | 高 | 🔧 | `OperationLog` 模型已有；本轮已添加核心业务操作日志。但**IP 记录不全**（仅部分接口），**查看/检索操作未记录**。 |
| 20 | 词库管理：支持自定义词库（单位简称、人名、专业术语），提升 OCR 识别准确率 | 高 | ❌ | 无词库管理功能。PaddleOCR 支持自定义词典但未暴露配置界面。 |
| 21 | 系统配置：档号规则、OCR 配置参数等可通过管理界面调整 | 中 | ❌ | 所有配置硬编码在 `.env` / Python 常量中。缺少管理界面。 |

## 五、导出功能（需求 22）

| # | 功能点 | 优先级 | 状态 | 说明 |
|---|--------|--------|------|------|
| 22 | 双层 PDF 导出：导出的 PDF 需保持原始扫描图片质量，上层为 OCR 识别后的隐藏文字层（可用于检索、复制），下层为原始扫描图片。文件名自动取"例：WS·2019·D10·001.pdf"。支持用户选择导出范围（单件、多件、按检索结果导出）。 | 高 | ❌ | 当前仅支持 Excel 导出。**双层 PDF 功能完全未实现**。需要 PyMuPDF 生成 searchable PDF。 |

---

## 统计汇总

| 状态 | 数量 | 占比 |
|------|------|------|
| ✅ 已完成 | 8 | 36% |
| 🔧 部分完成 | 8 | 36% |
| ❌ 未开始 | 6 | 28% |

---

## 开发优先级排序（建议）

### 第一轮（当前冲刺 — 核心交付）

| 优先级 | 需求 | 工作量 | 说明 |
|--------|------|--------|------|
| P0 | #22 双层PDF导出 | 3-4天 | 甲方高优先级；PyMuPDF 实现 searchable PDF |
| P0 | #04 批量修改 | 2天 | 后端 batch update API + 前端批量编辑 UI |
| P0 | #08 自动建文件夹 | 2天 | 根据档号规则创建物理目录 |
| P0 | #10 自动分件 | 3天 | 归档章检测→分件→移动图片→人工队列 |

### 第二轮（紧跟交付）

| 优先级 | 需求 | 工作量 | 说明 |
|--------|------|--------|------|
| P1 | #01 模板导入 | 1天 | Excel 反向导入功能 |
| P1 | #05 低置信度高亮 | 2天 | 前端 bbox 置信度渲染 + 修改日志 |
| P1 | #06 CSV导出 | 0.5天 | 简单格式转换 |
| P1 | #17 高级筛选 | 1.5天 | 搜索页筛选面板 UI |
| P1 | #19 操作日志完善 | 1天 | 补齐查看/检索日志 + IP |

### 第三轮（完善阶段）

| 优先级 | 需求 | 工作量 | 说明 |
|--------|------|--------|------|
| P2 | #07 规则配置界面 | 2天 | 管理员档号规则配置页 |
| P2 | #09 图片自动分拣 | 1.5天 | 依赖 #08 |
| P2 | #13 倒排索引 | 2天 | 可选 Elasticsearch 或 pg_trgm |
| P2 | #16 图片查看器 | 1.5天 | 独立查看器组件（放大/翻页） |
| P2 | #20 词库管理 | 2天 | 管理界面 + PaddleOCR 字典集成 |
| P2 | #21 系统配置 | 1.5天 | 管理员配置页面 |

VITE_CONTROL_PLANE_API_BASE_URL=http://localhost:8080
# Control plane / compute plane deployment (recommended for Java + Python split).
# VITE_AI_API_BASE_URL=http://localhost:8001
# Optional: override file/preview/download host if it differs from the API origin.
# VITE_FILE_BASE_URL=http://localhost:8080
# Optional: override control-plane file/download host if it differs from the control-plane API origin.
# VITE_CONTROL_PLANE_FILE_BASE_URL=http://localhost:8080
# Optional: override only AI-side file/preview/download host.
# VITE_AI_FILE_BASE_URL=http://localhost:8001
# Optional: override only the local Vite dev proxy target.
# VITE_DEV_API_PROXY_TARGET=http://localhost:8080

# Entities Layer

This layer hosts stable business entities and mapping helpers, for example:
- task
- batch
- archive record
- user

Purpose:
- Avoid duplicating data-shape mapping logic across pages and composables.

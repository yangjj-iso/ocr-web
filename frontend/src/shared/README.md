# Shared Layer

Reusable cross-feature modules:
- `api/`: shared HTTP client and request helpers
- `ui/`: reusable UI components
- `copy/`: unified business copy mapping and state-to-message mapping

Purpose:
- Keep cross-cutting code out of feature folders.
